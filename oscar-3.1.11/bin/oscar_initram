#!/bin/sh



## Copyright (c) 2003-2013 Tissoires Jean-François & Benjamin,  jftissoires@gmail.com
## Outil Système Complet d'Assistance Réseau: OSCAR
## Rapide de Sauvegarde aux Ordinateurs et Systèmes: RapideSOS
## Ce programme est sous Licence Publique Générale GNU publiée par la Free Software Foundation.


#	Il doit juste falloir rajouter ceci dans le script linuxrc:
#	mkdir ${NEW_ROOT}/mnt/livecd
#	mount -o loop /squashfs ${NEW_ROOT}/mnt/livecd

#	Au meme endroit que la ou il y a l'autre "mount -o loop" pour
#	${NEW_ROOT}/mnt/cdrom

#	1. decompresser
mkdir /mnt/temp
cd /mnt/temp
cp -f /livemnt/boot/isolinux/initram.igz /mnt/temp
cat initram.igz | gzip -d | cpio -id

#	2. modifier le fichier init (ou linuxrc)

#	3. recreer l'image
rm -f initram.igz
cd /mnt/temp
find . | cpio -H newc -o | gzip > ../initram.igz.new